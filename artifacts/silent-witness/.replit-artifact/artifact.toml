kind = "web"
previewPath = "/"
title = "Silent Witness"
version = "1.0.0"
id = "artifacts/silent-witness"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18661

[services.development]
run = "pnpm --filter @workspace/silent-witness run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/silent-witness", "run", "build" ]
publicDir = "artifacts/silent-witness/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18661"
BASE_PATH = "/"
